/**
 * Created by Lance on 2014/10/24.
 */
;(function($){
    $.fn.toTop = function(options){
        var defaults = {
            showHeight : 200,
            speed:1000
        };
        var options = $.extend(options,defaults);
        
    }
})(jQuery);